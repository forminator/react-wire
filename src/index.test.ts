import { ForminatorReactWire } from './index';

describe('ForminatorReactWire', () => {
  it('should return forminator-react-wire', () => {
    expect(ForminatorReactWire()).toBe('forminator-react-wire');
  });
});
